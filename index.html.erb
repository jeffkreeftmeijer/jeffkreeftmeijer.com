<h2>Ruby programmer, making apps at <a href="http://fngtps.com">Fingertips</a> in Amsterdam</h2>

<h4>Newest:</h4>
<ul class="articles">
  <% @articles.each do |article| %>
  <li>
    <h3><a href="<%= article.url %>"><%= article.metadata['title'] %></a></h3>
    <span class="date"><%= article.metadata['date'] %></span>
    <p><%= article.metadata['description'] %></p>
  </li>
<% end %>
</ul>

<p class="small more">
  Of course, there are <a href="/archive">more articles in the archive</a>.
</p>
